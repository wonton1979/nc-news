import {createContext} from "react";

export const PostCommentStateContext = createContext(undefined)

export const UsernameContext = createContext(undefined)

export const CurrentPageContext = createContext(undefined)

